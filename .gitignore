# Workspace project contents are gitignored (active document state)
# Each project folder and its contents are ignored
workspace/*/
workspace/.active-project

# Keep the workspace directory itself
!workspace/.gitkeep

# macOS
.DS_Store
